# Agent rules for this repository

- Do not create custom `golangci.yml` or `golangci.yaml` files. Ensure they are deleted if present.
- Go version must be at least 1.25. Never downgrade Go.
- Rely on external references and generic major tags for GitHub Action dependencies.
